
import './Banner.css'

const Banner = () => {

    // JSX - Não é HTML, é como o React lê e transforma no DOM
    return (
        <header className="banner">
            <img src="/imagens/banner.png" alt="Banner principal da página do Organo"/>
        </header>
    );
}

export default Banner;
